import { Playlist } from '@/services/musicService';
interface PlaylistCardProps {
  playlist: Playlist;
  onClick?: () => void;
}
const PlaylistCard = ({
  playlist,
  onClick
}: PlaylistCardProps) => {
  return;
};
export default PlaylistCard;